# Railway frontend: Root Directory = webapp (recommended) OR repo root
[build]
builder = "dockerfile"
dockerfilePath = "frontend/Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
