# Railway: set service "Root Directory" to repository root (not webapp/backend)
[build]
builder = "dockerfile"
dockerfilePath = "webapp/backend/Dockerfile"

[deploy]
startCommand = "./start.sh"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
